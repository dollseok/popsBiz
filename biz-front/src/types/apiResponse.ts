export type apiErrorType = {
  result: string;
  errorMsg: string;
  errorCode: string;
};

export type apiSuccessType = {
  result: string;
  payload: any;
  errorMsg: string;
  errorCode: string;
};
